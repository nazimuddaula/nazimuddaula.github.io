## Image sizing rule (critical, repeated many times)
Never force an `aspect-ratio` or fixed `height` on an `<image-slot>`. Set `width` only; height must stay proportionate to the actual source image (natural aspect ratio), not a preset ratio like 16/9 or 3/4. This applies to every image placed across all case studies and pages.
